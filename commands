git init
    Turns the current directory into a git repository.
git status
    Tells you the status of all files, including which files are being tracked and which are staged for commit.
git add <file>
    Tells git to include a specific file. Must be done for each edit.
git rm
    Tells git to stop tracking a specific file.
git commit -m "Commit Message"
    Commit your changes to the online repository.
git checkout -b "Branch_Name"
    Create a new Branch.
git checkout <branch>
    Switch to a different branch.
git branch
    Observe a list of all branches, and see which one you're in.
git branch -M "name"
    Rename current branch
git remote add origin https://github.com/<username>/<repositoryname>.git
    If you've created a repository online, link to it. Your username is "SecondPsycho".
git push -u origin <branchname>
    Push to the repository you've linkted to. Username and Password required.
git pull origin <branchname>
    Pull the specified branch from the website.
